<p style="color: green"><%= notice %></p>

<h1>Ideas</h1>
<!-- At the top we’ll have a new blue button with the label “Add a new idea”. -->
<%= link_to "Add a new idea", new_idea_path, class: "btn btn-primary mb-3" %>

<div class="list-group w-auto">
  <% @ideas.each do |idea| %>
    <%= render idea %>
  <% end %>
</div>